"use client";
import { useCallback, useState, type FormEvent } from "react"; import { Archive, Pencil, Plus, RotateCcw } from "lucide-react"; import { Button } from "@/components/ui/Button"; import { Dialog } from "@/components/ui/Dialog"; import { EmptyState, LoadingState, Notice } from "@/components/ui/Feedback"; import { Field } from "@/components/ui/Field"; import { PageHeader } from "@/components/ui/PageHeader"; import { useAuth } from "@/hooks/useAuth"; import { useCollectionData } from "@/hooks/useCollectionData"; import { createFriend, renameFriend, setFriendArchived, subscribeFriends } from "@/services/friends"; import type { Friend } from "@/types"; import { friendLabel } from "@/utils/format";
export default function FriendsPage() { const { currentUser } = useAuth(); const uid = currentUser!.uid; const subscribe = useCallback((next: Parameters<typeof subscribeFriends>[1], fail: Parameters<typeof subscribeFriends>[2]) => subscribeFriends(uid, next, fail), [uid]); const data = useCollectionData(subscribe); const [editing, setEditing] = useState<Friend | "new" | null>(null); const [showArchived, setShowArchived] = useState(false); const [busy, setBusy] = useState(false); const [message, setMessage] = useState<string | null>(null); const shown = data.items.filter(friend => showArchived || !friend.archived);
 async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); const name = String(new FormData(event.currentTarget).get("name")); try { if (editing === "new") await createFriend(uid, name); else if (editing) await renameFriend(uid, editing.id, name); setEditing(null); setMessage(editing === "new" ? "Friend added successfully." : "Friend renamed."); } catch (cause) { setMessage(cause instanceof Error ? cause.message : "Unable to save changes."); } finally { setBusy(false); } }
 async function archive(friend: Friend) { setBusy(true); try { await setFriendArchived(uid, friend, !friend.archived); setMessage(friend.archived ? "Friend restored." : "Friend archived."); } catch (cause) { setMessage(cause instanceof Error ? cause.message : "Unable to update friend."); } finally { setBusy(false); } }
 return <><PageHeader title="Friends" description="The people you split expenses with." action={<Button onClick={() => setEditing("new")}><Plus size={18} /> Add friend</Button>} /><div className="toolbar"><label><input type="checkbox" checked={showArchived} onChange={event => setShowArchived(event.target.checked)} /> Show archived</label></div><Notice message={data.error || message} tone={message ? "success" : "error"} />{data.loading ? <LoadingState /> : shown.length === 0 ? <EmptyState title="No friends yet" description="Add your first friend to start creating groups." /> : <div className="list">{shown.map(friend => <article className={`list-row ${friend.archived ? "muted" : ""}`} key={friend.id}><span className="avatar">{friend.name[0]?.toUpperCase()}</span><div><h2>{friendLabel(friend)}</h2><p>{friend.isMe ? "Your permanent profile" : friend.archived ? "Archived" : "Active friend"}</p></div><div className="row-actions"><button className="icon-button" aria-label={`Rename ${friend.name}`} onClick={() => setEditing(friend)}><Pencil size={18} /></button>{!friend.isMe && <button className="icon-button" aria-label={friend.archived ? `Restore ${friend.name}` : `Archive ${friend.name}`} disabled={busy} onClick={() => archive(friend)}>{friend.archived ? <RotateCcw size={18} /> : <Archive size={18} />}</button>}</div></article>)}</div>}<Dialog open={!!editing} title={editing === "new" ? "Add friend" : "Rename friend"} onClose={() => setEditing(null)}><form onSubmit={save} className="dialog-form"><Field label="Name" name="name" autoFocus required maxLength={80} defaultValue={editing && editing !== "new" ? editing.name : ""} /><div className="dialog-actions"><span /><Button type="button" variant="secondary" onClick={() => setEditing(null)}>Cancel</Button><Button disabled={busy}>{busy ? "Saving…" : "Save"}</Button></div></form></Dialog></>;
}
